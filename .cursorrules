# Cursor Rules for AMS Law Project

> **This file is legacy.** Project rules now live in `.cursor/rules/` as scoped `.mdc` files.
> See also `AGENTS.md` for a quick agent overview and `.cursor/skills/` for workflow guides.

## Quick links

| Topic | Rule file |
|-------|-----------|
| Colors & styling | `.cursor/rules/colors-and-styling.mdc` |
| Code style & imports | `.cursor/rules/code-style.mdc` |
| Testing | `.cursor/rules/testing.mdc` |
| Accessibility | `.cursor/rules/accessibility.mdc` |
| SEO | `.cursor/rules/seo.mdc` |
| Documentation | `.cursor/rules/documentation.mdc` |
| Commits & releases | `.cursor/rules/releases.mdc` |

When adding new patterns, update the relevant `.mdc` file in `.cursor/rules/` — not this file.
